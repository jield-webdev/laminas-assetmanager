before_commands:
    - "composer install --dev --prefer-source"

build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run
checks:
    php: true

imports:
    - php
